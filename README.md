# Big Bang

```bash
$ sudo apt-get install git build-essential vim zsh curl
$ chsh -s /bin/zsh
$ mkdir -p ~/workspace/github
$ git clone https://github.com/rhannequin/bigbang ~/workspace/github/bigbang
$ cd ~/workspace/github/bigbang
$ make init
$ make
```

Done.
